# Core dependencies
fitz
openai
PyMuPDF
PyPDF2==3.0.1
pdf2image>=1.16.3
pillow==10.2.0
plotly
poppler-utils
python-dotenv==1.0.0
streamlit #==1.32.0

# Development dependencies
ipykernel

# Note: This project requires Poppler to be installed separately:
# Windows: Download from: https://github.com/oschwartz10612/poppler-windows/releases/
# Linux: sudo apt-get install poppler-utils
# macOS: brew install poppler 